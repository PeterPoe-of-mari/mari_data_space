# Projections

A projection is a **derivable** response or view generated from:

- Values (facts)
- Mari Index (memory)
- Order Layer (origin/structure)
- Code of Law (norms)

Projections are context- and role-dependent and are not stored as an independent truth layer.
